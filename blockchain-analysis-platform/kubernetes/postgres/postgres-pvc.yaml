apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: postgres
spec:
  accessModes:
    - ReadWriteOnce  # Adjust if needed
  resources:
    requests:
      storage: 10Gi # Adjust storage size as needed